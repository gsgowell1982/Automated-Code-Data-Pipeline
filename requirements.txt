# --- 核心网络通信 ---
# 用于调用 Ollama 本地 API (http://localhost:11434)
requests>=2.31.0

# --- 类型增强与数据模型 (推荐) ---
# 确保在 Python 3.10 中对复杂 Schema 类型标注的完美支持
typing-extensions>=4.5.0
# 用于对生成的 JSONL 进行结构化校验（如 Data Quality 检查）
pydantic>=2.0.0

# --- 环境管理 ---
# 用于管理配置文件或环境变量，避免在代码中硬编码 API 地址
python-dotenv>=1.0.0

# --- 字符编码与处理 ---
# 确保处理源码和 JSON 时的健壮性
charset-normalizer>=3.0.0
